/** Defines the supported localizations and their shortcodes. */
export const LocaleCode = {
    English: "en",
};

/** Defines the supported localizations. */
export const LOCALES = Object.values(LocaleCode);